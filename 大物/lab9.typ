#import "ReportTemplate/phylab.typ": phylab

#show: phylab.with(
  name: "金属材料杨氏模量的测定",
  instructor: "张贯乔",
  desk-id: "20",
  class: "计科2404",
  author: "李宇晗",
  author-id: "3240106155",
  date: datetime(year: 2025, month: 11, day: 24),
  week: "第9周",
  am-pm: "上午",
)

// 此处填写正文，如：

= 预习报告（10分）

== 实验综述（5分）

1. 实验原理

杨氏模量 $E$ 是表征材料刚度的物理量，定义为在材料的弹性形变范围内，轴向应力与轴向应变的比值。

- 应力 $sigma = F / S$
- 应变 $epsilon = (delta L) / L$

因此，杨氏模量的计算公式为：
$ E = sigma / epsilon = (F dot L) / (S dot delta L) $

其中，$F$ 为施加的拉力，$L$ 为金属丝原长，$S$ 为横截面积，$delta L$ 为金属丝的伸长量。

2. 实验方法

由于金属丝在拉力作用下的伸长量 $delta L$ 非常微小，直接测量难以精确。本实验采用“光杠杆法”将其放大测量。光杠杆装置利用镜面反射原理，将微小的长度变化 $delta L$ 转化为反射光斑在标尺上一个较大的位移 $delta s$。

根据几何关系和“小角度近似”（$tan theta approx theta$），伸长量 $delta L$ 与标尺读数变化量 $delta s$ 的关系为：
$ delta L = (b dot delta s) / (2D) $

其中 $b$ 为光杠杆的短臂长（后足与前双足连线的垂直距离），$D$ 为镜面到标尺的距离。

将此关系代入杨氏模量公式，并考虑到 $F = m g$（$m$ 为砝码质量），$S = pi (d / 2)^2$（$d$ 为金属丝直径），得到最终的测量模型公式：
$ E = (8 D m g L) / (pi d^2 b delta s) $

实验中，通过逐次增加砝码来施加拉力，并利用望远镜读取标尺上的读数，记录多组数据。为减小误差，采用“最小二乘法”处理数据以求得 $Delta s \/ Delta m$ 的关系，最终计算出杨氏模量 $E$ 并进行不确定度分析。

== 实验重点（3分）
本次实验的重点在于掌握利用光杠杆法放大测量微小长度变化的原理与操作。准确使用米尺、游标卡尺、螺旋测微器等工具测量实验中的各个物理量（如 $D, b, L, d$）。同时，需要学习并运用逐差法或线性拟合对测量数据进行处理，以获得更精确的金属丝平均伸长量。

== 实验难点（2分）
本次实验的难点主要体现在实验装置的调节与系统误差的控制。光杠杆系统的调节尤为关键，包括将望远镜正确对焦、调节其与反射镜共轴以及消除视差，这需要细致耐心的操作。此外，如何确保支架的稳定、金属丝的竖直拉伸以及在加载砝码过程中避免晃动，是减小测量误差的关键。

= 原始数据（20分）
（将有老师签名的“自备数据记录草稿纸”的扫描或手机拍摄图粘贴在下方，完整保留姓名，学号，教师签字和日期。）

#image("/assets/8de31fd51d185f3fa4b5d28ea25af3ea.jpg", width: 70%)

= 结果与分析（60分）

== 数据处理与结果（30分）

=== 1. 基础物理量测量结果
对各基础物理量进行多次测量并求平均值：

#align(center)[
  #table(
    columns: (auto, auto, auto, auto, auto),
    fill: (_, row) => if calc.odd(row) { luma(240) } else { white },
    [*次数*], [*$L$ (cm)*], [*$D$ (cm)*], [*$b$ (mm)*], [*$d$ (mm)*],
    [1], [108.90], [135.62], [74.58], [0.649],
    [2], [108.86], [135.63], [74.60], [0.647],
    [3], [108.95], [135.51], [74.56], [0.651],
    [4], [108.78], [135.69], [74.58], [0.648],
    [5], [108.98], [135.57], [74.60], [0.649],
    [6], [108.84], [135.67], [74.58], [0.650],
    [*平均值*], [*108.89*], [*135.62*], [*74.58*], [*0.649*],
  )
]

- $overline(L) = 1.0889 text("m")$
- $overline(D) = 1.3562 text("m")$
- $overline(b) = 7.458 times 10^(-2) text("m")$
- $overline(d) = 6.49 times 10^(-4) text("m")$

=== 2. 标尺读数处理与线性拟合
计算每组砝码对应的标尺读数平均值 $s_(平 均)$，并进行线性拟合。

#align(center)[
  #table(
    columns: 4,
    fill: (_, row) => if calc.odd(row) { luma(240) } else { white },
    [*$m$ (kg)*], [*$s_(增)$*], [*$s_(减)$*], [*$s_(平 均)$ (mm)*],
    [1.00], [7.2], [6.9], [7.05],
    [2.00], [12.0], [13.2], [12.60],
    [3.00], [18.4], [18.8], [18.60],
    [4.00], [23.2], [23.2], [23.20],
    [5.00], [28.7], [28.2], [28.45],
    [6.00], [33.6], [34.1], [33.85],
    [7.00], [39.5], [38.1], [38.80],
    [8.00], [44.5], [43.8], [44.15],
  )
]

#image("/assets/youngs_modulus_fit-2.png")

通过线性回归分析 ($s = k m + c$)，得到拟合结果：
- 斜率 $k = 5.2583 text("mm/kg") = 5.2583 times 10^(-3) text("m/kg")$
- 截距 $c = 2.1750 text("mm")$
- 相关系数 $R^2 = 0.9995$

=== 3. 杨氏模量计算
将斜率 $k = (Delta s) / (Delta m)$ 代入公式：
$ E = (8 g L D) / (pi d^2 b k) $

代入数值：
- $g = 9.80 text("m/s")^2$
- $k = 5.2583 times 10^(-3) text("m/kg")$
- 其他参数见上文。

$
  E = (8 times 9.80 times 1.0889 times 1.3562) / (3.14159 times (6.49 times 10^(-4))^2 times 7.458 times 10^(-2) times 5.2583 times 10^(-3))
$

计算得：
$ E approx 2.23 times 10^(11) text("Pa") = 223 text("GPa") $

== 误差分析（20分）

=== 1. 不确定度评定
计算各直接测量量的标准不确定度：
1. $u(L)$ (钢卷尺, 1mm): $u(L) = 1 / sqrt(3) approx 0.58 text("mm") = 5.8 times 10^(-4) text("m")$
2. $u(D)$ (钢卷尺, 1mm): $u(D) = 1 / sqrt(3) approx 0.58 text("mm") = 5.8 times 10^(-4) text("m")$
3. $u(b)$ (游标卡尺, 0.02mm): $u(b) = 0.02 / sqrt(3) approx 0.012 text("mm") = 1.2 times 10^(-5) text("m")$
4. $u(d)$ (螺旋测微器, 0.004mm): $u(d) = 0.004 / sqrt(3) approx 0.0023 text("mm") = 2.3 times 10^(-6) text("m")$
5. $u(k)$ (斜率, A类): 根据线性拟合结果，标准误差 $u(k) approx 0.0502 text("mm/kg") = 5.02 times 10^(-5) text("m/kg")$

=== 2. 合成相对不确定度
根据不确定度传播公式：
$ (u(E) / E)^2 = (u(L) / L)^2 + (u(D) / D)^2 + (2 u(d) / d)^2 + (u(b) / b)^2 + (u(k) / k)^2 $

代入各分量相对不确定度的平方：
1. $(u(L) / L)^2 approx (5.8 times 10^(-4) / 1.0889)^2 approx 2.8 times 10^(-7)$
2. $(u(D) / D)^2 approx (5.8 times 10^(-4) / 1.3562)^2 approx 1.8 times 10^(-7)$
3. $(2 u(d) / d)^2 approx (2 times 2.3 times 10^(-6) / (6.49 times 10^(-4)))^2 approx 5.0 times 10^(-5)$
4. $(u(b) / b)^2 approx (1.2 times 10^(-5) / (7.458 times 10^(-2)))^2 approx 2.6 times 10^(-8)$
5. $(u(k) / k)^2 = (5.02 times 10^(-5) / (5.2583 times 10^(-3)))^2 approx 9.1 times 10^(-5)$

总相对不确定度：
$ u(E) / E = sqrt(5.0 times 10^(-5) + 9.1 times 10^(-5)) approx sqrt(1.41 times 10^(-4)) approx 0.0119 approx 1.2% $

=== 3. 最终结果表达式
绝对不确定度 $u(E) = E times 1.2% approx 2.23 times 10^(11) times 0.012 approx 0.03 times 10^(11) text("Pa")$

实验结果为：
$ E = (2.23 plus.minus 0.03) times 10^(11) text("Pa") $

=== 4. 误差原因分析
本次实验测得的杨氏模量为 223 GPa，略高于钢材标准值（约 200-210 GPa）。主要误差来源包括：
1. 随机误差：望远镜读数时的估读误差、视差以及加载砝码时的晃动是 $k$ 值不确定度（约 1%）的主要来源。
2. 系统误差：
  - $b$ 的测量误差：光杠杆短臂 $b$ 较短，微小的测量误差（如测量值偏小）会导致结果 $E$ 显著偏大。
  - $d$ 的测量误差：金属丝直径平方项 $(d^2)$ 对结果影响最大，若直径测量处有压缩或测量值偏小，会导致 $E$ 偏大。
  - 仪器垂直度：标尺面若未严格垂直于望远镜光轴，会导致读数变化量 $ Delta s$ 测量不准。

== 实验探讨（10分）
（对实验内容、现象和过程的小结。）

本实验成功利用光杠杆放大拉伸法测定了金属丝的杨氏模量。通过对砝码质量与标尺读数进行线性拟合，验证了在弹性限度内应力与应变成正比的胡克定律。最终测得 $E = (2.23 plus.minus 0.03) times 10^(11) text("Pa")$。实验结果表明，光杠杆法能有效放大微小形变，但也对 $b$ 和 $d$ 的测量精度提出了极高要求。

= 思考题（10分）

1. 本实验中测量了哪些物理量？分别用什么量具进行测量？

答：
- 金属丝原长 L：使用钢卷尺，单位 cm，记录到毫米位。
- 镜面到标尺距离 D：使用钢卷尺，单位 cm，记录到毫米位。
- 光杠杆短臂长 b：使用游标卡尺，单位 mm，记录到 0.02mm 位。
- 金属丝直径 d：使用螺旋测微器，单位 mm，记录到 0.001mm 位。
- 标尺读数 s：通过望远镜读取，单位 mm，估读到 0.1mm 位。
- 砝码质量 m：使用标准砝码，质量已知。

2. 光杠杆中，增大 D 和减小 b 都可以增加放大倍数，那么两者有何不同？是否可以无限放大光杠杆的倍数？

答：
- 不同之处：增大 $D$ 会使装置占用空间变大，对环境稳定性要求更高，且可能导致光斑变暗、变大；减小 $b$ 受限于光杠杆的物理结构，过小的 $b$ 会导致装置不稳定。
- 能否无限放大：不能。放大倍数受限于光的衍射效应（光斑模糊）、光斑亮度下降、环境振动被同步放大以及仪器本身的加工精度。

3. 逐差法、作图法、最小二乘法在处理数据时有何不同？

答：
- 逐差法：适用于自变量等间隔变化的数据，能利用全部数据减小随机误差，计算简便。
- 作图法：直观展示变量关系，易于发现坏点，但拟合直线的绘制具有主观性，结果依赖于作图精度。
- 最小二乘法：基于误差平方和最小化原理，是数学上最严谨的方法，能提供斜率和截距的最佳估计值及其不确定度，客观性最强。